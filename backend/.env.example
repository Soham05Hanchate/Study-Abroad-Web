# Groq
GROQ_API_KEY=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

# MongoDB Atlas (optional — server runs without it; extract won't persist students)
# Use Database → Connect → Drivers → copy SRV string. Replace password; set DB name in path.
MONGODB_URI=mongodb+srv://soham:sohamh@cluster0.d20xuue.mongodb.net/?appName=Cluster0

# Optional: exit if URI is set but connection fails (production)
# REQUIRE_MONGODB=true

# Optional: require Bearer or X-API-Key for POST /api/counsellors and POST|PATCH /api/knowledge
# ADMIN_API_KEY=your-long-random-secret

PORT=5000
